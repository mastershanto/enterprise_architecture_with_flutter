abstract class EquipmentLocalDataSource {
  // Future methods for caching if needed
}

class EquipmentLocalDataSourceInMemory implements EquipmentLocalDataSource {
  // In-memory cache implementation if needed
}
